# ── DairyVision AI – Python Dependencies ─────────────────────────────────────
#
# Install with:
#     pip install -r requirements.txt
#
# Compatible with Python 3.10+ including 3.14

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0         # async Python web framework
uvicorn[standard]>=0.29.0  # ASGI server

# ── Data science ──────────────────────────────────────────────────────────────
scikit-learn>=1.4.0      # RandomForestRegressor + RandomForestClassifier
pandas>=2.0.0            # DataFrame operations
numpy>=1.26.0            # numeric arrays

# ── Model serialisation ───────────────────────────────────────────────────────
joblib>=1.3.0            # fast model save/load

# ── Pydantic (used by FastAPI for request validation) ─────────────────────────
pydantic>=2.5.0          # v2-compatible data validation

# ── Development & testing ─────────────────────────────────────────────────────
httpx>=0.27.0            # async HTTP client used by FastAPI's TestClient
pytest>=8.0.0            # test runner
pytest-asyncio>=0.23.0   # async test support
